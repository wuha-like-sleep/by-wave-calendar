## 改了什么

<!-- 一句话说清楚 -->

## 为什么

<!-- 背景 / 触发这次改动的问题。比「改了什么」更重要 -->

## 自测

- [ ] `npx tsc -p tsconfig.json --noEmit` 通过
- [ ] `npx vitest run` 通过
- [ ] 改了用户可见文案 → 8 种语言都补了(`npm run i18n:check`)
- [ ] 改了自然语言词库 → 服务端 `nl_parse.ts` 与网页端 `calendar-app.js` 两处已同步
- [ ] 改了原生端 → 版本号两处 + `releases/latest.json` 已同步

## 截图 / 录屏

<!-- 涉及界面的话请附上,前后对比最好 -->
