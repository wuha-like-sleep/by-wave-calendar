// Typography. We don't ship a custom font — Roboto on Android matches
// system look-and-feel, and the iOS APP also uses the system font (SF
// Pro), so there's no brand-consistency reason to override.
//
// Material 3 defaults are fine; this file exists so future tweaks have
// a clear home.

package cn.bywave.calendar.ui.theme

import androidx.compose.material3.Typography

val Typography: Typography = Typography()
