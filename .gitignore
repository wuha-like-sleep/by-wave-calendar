node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store
release/
drizzle/migrations/meta/_journal.json.bak

# Build outputs (regenerated by `npm run build:assets`)
src/public/lib/
src/public/css/styles.css
src/public/_built/

# User-uploaded files
src/public/uploads/

# Native APP starter source — kept local only, not pushed to public
# remote. The server-side device-pairing API (Phase 1) stays in this
# repo; the app implementation lives in a separate private workspace.
apps/ios/
apps/android/
apps/desktop/
