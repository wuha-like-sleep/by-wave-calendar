<nav class="mb-4 text-sm text-slate-500">
  <a href="/app" class="hover:text-slate-700">我的日历</a>
  <span class="px-2">/</span>
  <span class="text-slate-900"><%= calendar.name %></span>
</nav>

<header class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-6 mb-6">
  <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-4">
    <div class="flex items-start gap-3 min-w-0">
      <span class="inline-block h-10 w-10 rounded-lg flex-shrink-0" style="background:<%= calendar.color %>"></span>
      <div class="min-w-0">
        <h1 class="text-xl sm:text-2xl font-semibold text-slate-900 truncate"><%= calendar.name %></h1>
        <% if (calendar.description) { %>
          <p class="mt-1 text-sm text-slate-500"><%= calendar.description %></p>
        <% } %>
        <p class="mt-1 text-xs text-slate-400">时区 <%= calendar.timezone %></p>
      </div>
    </div>
    <div class="flex flex-wrap gap-2">
      <form method="post" action="/app/calendars/<%= calendar.id %>/delete" data-confirm="确定删除整个日历？此操作不可恢复。" data-confirm-danger="1" data-confirm-ok="删除日历" data-confirm-title="删除日历" class="m-0">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <button type="submit" class="rounded-lg border border-red-200 bg-white px-3 py-1.5 text-sm text-red-700 hover:bg-red-50">删除日历</button>
      </form>
    </div>
  </div>
</header>

<div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">

<section class="rounded-2xl border border-slate-200 bg-white p-4 sm:p-5">
  <details>
    <summary class="cursor-pointer flex items-center justify-between gap-2">
      <span class="text-base font-semibold text-slate-900">👥 邀请协作 <% if (members && members.length > 0) { %><span class="ml-1 text-xs font-normal text-slate-400">· <%= members.length %> 位成员</span><% } %></span>
      <span class="text-xs text-slate-400">展开 ▾</span>
    </summary>
    <div class="mt-3 text-sm text-slate-500 mb-3">通过邮箱邀请他人加入这本日历。对方接受后会出现在他们的日历列表里。</div>

  <form method="post" action="/app/calendars/<%= calendar.id %>/invitations" class="grid grid-cols-1 sm:grid-cols-[1fr_auto_auto] gap-2 mb-3">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    <input type="email" name="email" required maxlength="254" placeholder="同事 / 家人的邮箱"
           class="rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100" />
    <select name="role" class="rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100">
      <option value="viewer">只读查看</option>
      <option value="editor">可编辑</option>
    </select>
    <button type="submit" class="rounded-lg bg-brand-600 px-4 py-2 text-sm font-medium text-white hover:bg-brand-700">发送邀请</button>
  </form>
  <textarea name="message" placeholder="留言（可选，会显示在邀请邮件里）" maxlength="500" rows="2"
            class="hidden w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100"></textarea>

  <% if (members && members.length > 0) { %>
    <div class="mt-2">
      <h3 class="text-xs font-semibold text-slate-500 uppercase tracking-wide mb-2">当前成员</h3>
      <ul class="divide-y divide-slate-100 border-t border-slate-100">
        <% members.forEach(function(m){ %>
          <li class="py-2.5 flex items-center justify-between gap-3">
            <div class="min-w-0">
              <div class="text-sm font-medium text-slate-900"><%= m.displayName || m.email %></div>
              <div class="text-xs text-slate-500"><%= m.email %> · <%= m.role === 'editor' ? '可编辑' : '只读' %></div>
            </div>
            <form method="post" action="/app/calendars/<%= calendar.id %>/members/<%= m.id %>/remove" class="m-0" data-confirm="确定移除该成员？" data-confirm-danger="1" data-confirm-ok="移除">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <button type="submit" class="text-xs text-red-600 hover:text-red-700 px-2 py-1">移除</button>
            </form>
          </li>
        <% }) %>
      </ul>
    </div>
  <% } %>

  <% if (pendingInvitations && pendingInvitations.length > 0) { %>
    <div class="mt-4">
      <h3 class="text-xs font-semibold text-slate-500 uppercase tracking-wide mb-2">待接受的邀请</h3>
      <ul class="divide-y divide-slate-100 border-t border-slate-100">
        <% pendingInvitations.forEach(function(inv){ %>
          <li class="py-2.5 flex items-center justify-between gap-3">
            <div class="min-w-0">
              <div class="text-sm font-medium text-slate-900"><%= inv.email %></div>
              <div class="text-xs text-slate-500"><%= inv.role === 'editor' ? '可编辑' : '只读' %> · 到期 <%- inv.expiresAtLocal %></div>
            </div>
            <form method="post" action="/app/calendars/<%= calendar.id %>/invitations/<%= encodeURIComponent(inv.token) %>/revoke" class="m-0" data-confirm="撤销这条邀请？" data-confirm-danger="1" data-confirm-ok="撤销">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <button type="submit" class="text-xs text-red-600 hover:text-red-700 px-2 py-1">撤销</button>
            </form>
          </li>
        <% }) %>
      </ul>
    </div>
  <% } %>
  </details>
</section>

<section class="rounded-2xl border border-slate-200 bg-white p-4 sm:p-5">
  <details>
    <summary class="cursor-pointer flex items-center justify-between gap-2">
      <span class="text-base font-semibold text-slate-900">🔗 订阅链接 <% if (shareTokens && shareTokens.length > 0) { %><span class="ml-1 text-xs font-normal text-slate-400">· <%= shareTokens.length %> 条</span><% } %></span>
      <span class="text-xs text-slate-400">展开 ▾</span>
    </summary>
    <div class="mt-3 text-sm text-slate-500 mb-3">生成后把链接粘贴到 Google / Apple / Outlook 日历的「添加订阅日历」即可。</div>

    <!-- One-shot snapshot download: 你的当前日历状态打包成 .ics，不需要订阅链接也不需要撤销。
         适合"我就是想把这本日历存档/给朋友/迁移到其他工具"的场景。 -->
    <a href="/app/calendars/<%= calendar.id %>/export.ics"
       class="inline-flex items-center gap-2 rounded-lg border border-slate-200 bg-slate-50 px-3 py-1.5 text-sm text-slate-700 hover:bg-slate-100 mb-4"
       download>
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4M7 10l5 5 5-5M12 15V3"/></svg>
      下载这本日历的 .ics 快照
    </a>

  <form method="post" action="/app/calendars/<%= calendar.id %>/share-tokens" class="flex flex-col sm:flex-row gap-2 mb-4">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    <input type="text" name="label" maxlength="100" placeholder="备注（如：给家人）"
           class="flex-1 rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100" />
    <button type="submit" class="rounded-lg bg-brand-600 px-4 py-2 text-sm font-medium text-white hover:bg-brand-700">生成订阅链接</button>
  </form>

  <% if (shareTokens.length === 0) { %>
    <p class="text-sm text-slate-400 text-center py-4">还没有订阅链接</p>
  <% } else { %>
    <ul class="divide-y divide-slate-100 border-t border-slate-100">
      <% shareTokens.forEach(function(t){ %>
        <li class="py-3 flex flex-col sm:flex-row sm:items-center gap-2 sm:gap-3">
          <div class="min-w-0 flex-1">
            <div class="text-sm font-medium text-slate-700"><%= t.label || '未命名' %></div>
            <div class="mt-1 flex items-center gap-2">
              <code class="text-xs text-slate-500 truncate flex-1 bg-slate-50 px-2 py-1 rounded"><%= t.url %></code>
            </div>
          </div>
          <div class="flex gap-2 flex-shrink-0 flex-wrap">
            <button type="button" class="rounded-lg border border-slate-200 bg-white px-3 py-1.5 text-xs text-slate-700 hover:bg-slate-50"
                    onclick="window.bwc.copy('<%= t.url %>')">复制 ICS</button>
            <button type="button" class="rounded-lg border border-slate-200 bg-white px-3 py-1.5 text-xs text-slate-700 hover:bg-slate-50"
                    onclick="window.bwc.copy('&lt;iframe src=&quot;<%= t.url.replace('/ics/', '/embed/').replace('.ics', '') %>&quot; width=&quot;100%&quot; height=&quot;600&quot; frameborder=&quot;0&quot;&gt;&lt;/iframe&gt;')">复制嵌入代码</button>
            <form method="post" action="/app/calendars/<%= calendar.id %>/share-tokens/<%= t.token %>/revoke" class="m-0" data-confirm="确定撤销该订阅链接？已订阅的客户端会失效。" data-confirm-danger="1" data-confirm-ok="撤销">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <button type="submit" class="rounded-lg border border-red-200 bg-white px-3 py-1.5 text-xs text-red-700 hover:bg-red-50">撤销</button>
            </form>
          </div>
        </li>
      <% }) %>
    </ul>
  <% } %>
  </details>
</section>

</div><!-- /two-col grid -->

<section class="rounded-2xl border border-slate-200 bg-white p-4 sm:p-5 mb-6">
  <details>
    <summary class="cursor-pointer flex items-center justify-between gap-2">
      <span class="text-base font-semibold text-slate-900">📥 从其他平台导入 <% if (subscriptions && subscriptions.length > 0) { %><span class="ml-1 text-xs font-normal text-slate-400">· <%= subscriptions.length %> 个订阅</span><% } %></span>
      <span class="text-xs text-slate-400">展开 ▾</span>
    </summary>
    <div class="mt-3 text-sm text-slate-500 mb-3">四种方式：上传 .ics 文件、粘贴 ICS 文本、一次性从 URL 拉取、添加订阅（服务器按周期自动同步）。</div>

  <div class="mb-3 flex flex-wrap gap-2 border-b border-slate-200" id="import-tabs">
    <button type="button" data-tab="file"  class="import-tab px-3 py-2 text-sm font-medium border-b-2 border-brand-600 text-brand-700">📁 文件</button>
    <button type="button" data-tab="url"   class="import-tab px-3 py-2 text-sm font-medium border-b-2 border-transparent text-slate-500 hover:text-slate-700">🔗 URL 一次性</button>
    <button type="button" data-tab="sub"   class="import-tab px-3 py-2 text-sm font-medium border-b-2 border-transparent text-slate-500 hover:text-slate-700">🔁 订阅自动同步</button>
    <button type="button" data-tab="paste" class="import-tab px-3 py-2 text-sm font-medium border-b-2 border-transparent text-slate-500 hover:text-slate-700">📝 粘贴文本</button>
  </div>

  <div data-pane="file" class="import-pane">
    <form method="post" action="/app/calendars/<%= calendar.id %>/import/file" enctype="multipart/form-data" class="flex flex-col sm:flex-row gap-2">
      <input type="file" name="file" accept=".ics,text/calendar" required
             class="flex-1 rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm file:mr-3 file:rounded-md file:border-0 file:bg-slate-100 file:px-3 file:py-1 file:text-sm file:text-slate-700 hover:file:bg-slate-200" />
      <button type="submit" class="rounded-lg bg-brand-600 px-4 py-2 text-sm font-medium text-white hover:bg-brand-700">上传并导入</button>
    </form>
    <p class="mt-2 text-xs text-slate-400">从 Google Calendar 设置 → 导出，或 Apple 日历 → 文件 → 导出，得到 .ics 文件。</p>
  </div>

  <div data-pane="url" class="import-pane hidden">
    <form method="post" action="/app/calendars/<%= calendar.id %>/import/url-once" class="flex flex-col sm:flex-row gap-2">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
      <input type="url" name="url" required placeholder="https://… 或 webcal://…"
             class="flex-1 rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100" />
      <button type="submit" class="rounded-lg bg-brand-600 px-4 py-2 text-sm font-medium text-white hover:bg-brand-700">立即导入</button>
    </form>
    <p class="mt-2 text-xs text-slate-400">一次性从远程 URL 拉取并导入，之后不再自动同步。适合公开节假日 / 课表的快照。</p>
  </div>

  <div data-pane="sub" class="import-pane hidden">
    <form method="post" action="/app/calendars/<%= calendar.id %>/subscriptions" class="space-y-2">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
      <div class="flex flex-col sm:flex-row gap-2">
        <input type="url" name="url" required placeholder="远程 ICS URL（https:// 或 webcal://）"
               class="flex-1 rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100" />
        <button type="submit" class="rounded-lg bg-brand-600 px-4 py-2 text-sm font-medium text-white hover:bg-brand-700">添加订阅</button>
      </div>
      <div class="flex flex-col sm:flex-row gap-2">
        <input type="text" name="label" maxlength="60" placeholder="备注（例如：公司日历 / 团队课表）"
               class="flex-1 rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100" />
        <select name="refreshMinutes"
                class="rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100">
          <option value="60">每小时同步</option>
          <option value="360" selected>每 6 小时同步</option>
          <option value="720">每 12 小时同步</option>
          <option value="1440">每天同步</option>
          <option value="10080">每周同步</option>
        </select>
      </div>
    </form>
    <p class="mt-2 text-xs text-slate-400">服务器按周期拉取并增量更新事件（同 UID 即更新，新 UID 即新增）。删除订阅不会删除已导入的事件。</p>

    <% if (subscriptions && subscriptions.length > 0) { %>
      <ul class="mt-4 divide-y divide-slate-100 border-t border-slate-100">
        <% subscriptions.forEach(function(s){ %>
          <li class="py-3 flex items-center justify-between gap-3">
            <div class="min-w-0">
              <div class="text-sm font-medium text-slate-900 truncate"><%= s.label || s.url %></div>
              <div class="text-xs text-slate-500 break-all"><%= s.url %></div>
              <div class="text-xs mt-1">
                <% if (s.lastStatus === 'ok') { %>
                  <span class="text-emerald-600">✓ 上次同步 <%- s.lastFetchedAtLocal %> · <%= s.lastEventCount %> 个事件</span>
                <% } else if (s.lastStatus === 'error') { %>
                  <span class="text-red-600">✗ <%- s.lastFetchedAtLocal %> 失败：<%= s.lastError %></span>
                <% } else { %>
                  <span class="text-slate-400">尚未同步</span>
                <% } %>
                · 每 <%= s.refreshMinutes %> 分钟
              </div>
            </div>
            <div class="flex gap-2 shrink-0">
              <form method="post" action="/app/calendars/<%= calendar.id %>/subscriptions/<%= s.id %>/refresh" class="m-0">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <button type="submit" class="text-xs text-brand-600 hover:text-brand-700 px-2 py-1">立即同步</button>
              </form>
              <form method="post" action="/app/calendars/<%= calendar.id %>/subscriptions/<%= s.id %>/delete" class="m-0" data-confirm="删除这个订阅？已导入的事件会保留，但不再自动同步。" data-confirm-danger="1" data-confirm-ok="删除">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <button type="submit" class="text-xs text-red-600 hover:text-red-700 px-2 py-1">删除</button>
              </form>
            </div>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </div>

  <div data-pane="paste" class="import-pane hidden">
    <form method="post" action="/app/calendars/<%= calendar.id %>/import/text" class="space-y-2">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
      <textarea name="text" required rows="6" placeholder="BEGIN:VCALENDAR&#10;VERSION:2.0&#10;…"
                class="w-full rounded-lg border border-slate-300 px-3 py-2 text-xs font-mono focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100"></textarea>
      <button type="submit" class="rounded-lg bg-brand-600 px-4 py-2 text-sm font-medium text-white hover:bg-brand-700">导入文本</button>
    </form>
    <p class="mt-2 text-xs text-slate-400">如果你只能复制内容（比如管理员给的一段 ICS 文本），粘到这里就能导入。</p>
  </div>
  </details>
</section>

<script nonce="<%= typeof cspNonce !== 'undefined' ? cspNonce : '' %>">
(function(){
  const tabs = document.querySelectorAll(".import-tab");
  const panes = document.querySelectorAll(".import-pane");
  tabs.forEach(t => t.addEventListener("click", () => {
    const name = t.getAttribute("data-tab");
    tabs.forEach(x => {
      const active = x.getAttribute("data-tab") === name;
      x.classList.toggle("border-brand-600", active);
      x.classList.toggle("text-brand-700", active);
      x.classList.toggle("border-transparent", !active);
      x.classList.toggle("text-slate-500", !active);
    });
    panes.forEach(p => p.classList.toggle("hidden", p.getAttribute("data-pane") !== name));
  }));
})();
</script>

<section class="rounded-2xl border border-slate-200 bg-white p-5 sm:p-6">
  <div class="flex flex-wrap items-center justify-between gap-2 mb-4">
    <h2 class="text-lg font-semibold text-slate-900">事件</h2>
    <div class="flex items-center gap-2">
      <input type="date" id="event-filter-from"
             class="rounded-lg border border-slate-300 px-2 py-1 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100" />
      <label class="inline-flex items-center gap-1 text-xs text-slate-600 cursor-pointer">
        <input type="checkbox" id="event-show-past" class="h-3.5 w-3.5 rounded border-slate-300 accent-brand-600" />
        显示过去事件
      </label>
      <button type="button" onclick="document.getElementById('new-event-form').classList.toggle('hidden')"
              class="inline-flex items-center gap-1 rounded-lg bg-brand-600 px-3 py-1.5 text-sm font-medium text-white hover:bg-brand-700">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg>
        新建事件
      </button>
    </div>
  </div>

  <form id="new-event-form" method="post" action="/app/calendars/<%= calendar.id %>/events" class="hidden mb-4 grid sm:grid-cols-2 gap-3 rounded-lg bg-slate-50 p-4">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    <div class="sm:col-span-2">
      <label class="block text-xs font-medium text-slate-600 mb-1">标题</label>
      <input type="text" name="summary" required maxlength="500"
             class="block w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100" />
    </div>
    <div>
      <label class="block text-xs font-medium text-slate-600 mb-1">开始时间</label>
      <input type="datetime-local" name="startsAt" required
             class="block w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100" />
    </div>
    <div>
      <label class="block text-xs font-medium text-slate-600 mb-1">结束时间</label>
      <input type="datetime-local" name="endsAt" required
             class="block w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100" />
    </div>
    <div class="sm:col-span-2">
      <label class="block text-xs font-medium text-slate-600 mb-1">地点（可选）</label>
      <input type="text" name="location" maxlength="500"
             class="block w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100" />
    </div>
    <div class="sm:col-span-2">
      <label class="block text-xs font-medium text-slate-600 mb-1">描述（可选）</label>
      <textarea name="description" maxlength="5000" rows="2"
                class="block w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-100"></textarea>
    </div>
    <div class="sm:col-span-2 flex justify-end gap-2">
      <button type="button" onclick="document.getElementById('new-event-form').classList.add('hidden')"
              class="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm text-slate-700 hover:bg-slate-50">取消</button>
      <button type="submit" class="rounded-lg bg-brand-600 px-4 py-2 text-sm font-medium text-white hover:bg-brand-700">添加</button>
    </div>
  </form>

  <% if (events.length === 0) { %>
    <p class="text-sm text-slate-400 text-center py-8">还没有事件</p>
  <% } else { %>
    <ul id="event-list" class="divide-y divide-slate-100">
      <% events.forEach(function(ev){ %>
        <li class="event-row py-3 flex items-start justify-between gap-3"
            data-starts="<%= ev.startsAt.toISOString() %>"
            data-ends="<%= ev.endsAt.toISOString() %>">
          <div class="min-w-0 flex-1">
            <div class="text-sm font-medium text-slate-900"><%= ev.summary %></div>
            <div class="mt-0.5 text-xs text-slate-500">
              <%- ev.startsAtLocal %> → <%- ev.endsAtLocal %>
              <% if (ev.location) { %> · <%= ev.location %><% } %>
            </div>
            <% if (ev.description) { %>
              <p class="mt-1 text-xs text-slate-500 whitespace-pre-wrap"><%= ev.description %></p>
            <% } %>
          </div>
          <form method="post" action="/app/events/<%= ev.id %>/delete" class="m-0 flex-shrink-0" data-confirm="删除该事件？" data-confirm-danger="1" data-confirm-ok="删除">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
            <button type="submit" class="text-xs text-red-600 hover:text-red-700 px-2 py-1">删除</button>
          </form>
        </li>
      <% }) %>
    </ul>
    <p id="event-empty-msg" class="hidden text-sm text-slate-400 text-center py-8">该时间段内没有事件</p>
  <% } %>
</section>

<script nonce="<%= typeof cspNonce !== 'undefined' ? cspNonce : '' %>">
(function(){
  // Default filter: today onwards, past events hidden. User can change the
  // "from" date or tick "显示过去事件" to widen the window.
  const fromEl = document.getElementById("event-filter-from");
  const pastEl = document.getElementById("event-show-past");
  const list = document.getElementById("event-list");
  const empty = document.getElementById("event-empty-msg");
  if (!fromEl || !list) return;

  const today = new Date(); today.setHours(0, 0, 0, 0);
  fromEl.value = today.toISOString().slice(0, 10);

  function apply() {
    const fromISO = fromEl.value ? new Date(fromEl.value + "T00:00:00") : null;
    const showPast = pastEl.checked;
    let visible = 0;
    list.querySelectorAll(".event-row").forEach((row) => {
      const ends = new Date(row.dataset.ends);
      const starts = new Date(row.dataset.starts);
      const inFuture = ends >= today;
      const afterFrom = fromISO ? starts >= fromISO : true;
      const keep = showPast ? afterFrom : (afterFrom && inFuture);
      row.classList.toggle("hidden", !keep);
      if (keep) visible++;
    });
    if (empty) empty.classList.toggle("hidden", visible > 0);
  }
  fromEl.addEventListener("change", apply);
  pastEl.addEventListener("change", apply);
  apply();
})();
</script>
