# 服务端 1.7.2

> 取代 1.7.1。这一版做的是一件事：**让你自己的服务器发客户端安装包**。
> 如果你不打算这么做，这一版对你没有影响，正常升级即可，什么都不用配。

---

## 一、想用这个功能的话，先改一行 nginx

**只有你要从自己服务器发安装包时才需要改。** 不用这个功能就跳过这一节。

你的 nginx 里这一项默认是 `10m`：

```nginx
client_max_body_size 320m;
proxy_request_buffering off;
proxy_send_timeout 600s;
proxy_read_timeout 600s;
```

不改的话，上传 100MB 级别的安装包**连本程序都到不了** —— nginx 自己返回 413 并切断连接，浏览器只看到一个没头没尾的失败，服务端日志里什么都没有，很容易被当成「后台上传坏了」。

顺带说明：这一项以前在部署示例里就是 `10m`，也就是说照文档装的站，**连原有那条 100MB 的「系统更新」上传也从来没能成功过**。只是大多数人的更新包只有一两 MB，所以没撞上。

（宝塔面板：网站 → 设置 → 配置文件，找到 `client_max_body_size` 改掉，保存后重载 nginx。）

### 宝塔还有第二道闸

改完 nginx 之后，如果上传还是失败、而且失败得很奇怪（**状态码是 200，但页面提示上传没成功**），那是宝塔的 **Nginx 防火墙**在拦——它会去逐字节检查请求体，100MB 的安装包必然超出它的缓冲区，提示是「Nginx缓冲区溢出」。

解决：宝塔 → **Nginx 防火墙** → **URL 白名单**，加这两条：

```
^/admin/client-release/.*/binary$
^/admin/update/upload$
```

第二条是顺手加的：你后台「系统更新」上传走的是同一套 WAF，包一旦超过缓冲区会撞同样的墙。

加白名单是安全的选择——这两个地址本来就只有管理员能用（WAF 之后还有一层登录校验），而让 WAF 去逐字节检查一个二进制安装包，既拦不住什么，又必然溢出。

---

## 二、后台新增：客户端更新

**运维 → 客户端更新**。这一页现在能做完整件事：

| 你能做什么 | 说明 |
|---|---|
| 看见服务器上有哪些安装包 | 每个多大、清单认不认它（「正在用」/「清单没引用它」/「上传残留」） |
| 传新的 | 选文件就开始，带进度条 |
| 删旧的 | 每个包右边一个删除按钮 |
| 看磁盘剩余 | 低于 3GB 会变黄 |

**做完这件事，安装包这件事就不用再开宝塔的文件管理器了。**

### 上传时会替你挡一件事

传上来的包会**在保存之前**和清单里的 sha256 比对。对不上就不保存，并把「清单里写的」和「你传上来的」两个值并排摆出来。

为什么这一步重要：客户端下载完会校验这个值，不匹配就删掉重下 —— 用户陷入无限重试，而**你完全不知道为什么**，只会收到「更新不了」的反馈。

清单里没写 sha256 的话不会默默放行，会明确告诉你「这一版发出去不会有任何客户端校验」。

---

## 三、从本站发包，现在对 App 也生效了

上一版（1.7.1）只做了一半：**网页的下载按钮**会优先用本站的文件，而 **App 内的自动更新仍然去 GitHub 拉**。

也就是说传到服务器上的包只对新用户有用，而已装机的老用户 —— 恰恰是最需要从本站下载的那一半 —— 一个都没受益。这一版修好了。

还补上了两件对 100MB 级别的下载很要紧的：

- **断点续传**。以前网断一次就得从 0 重下 109MB，而「从本站发包」存在的全部理由就是给从 GitHub 下不动的人一条路。
- **不再被 HEAD 请求白白消耗**。以前一个几百字节的 HEAD 请求会让服务器把整个安装包从磁盘读一遍再丢掉，出站流量 0。

---

## 四、怎么用起来

1. 按第一节改 nginx。
2. 后台 **运维 → 客户端更新**，把安装包传上去（文件名要和清单里的一致 —— 页面上会显示清单期望的是哪个名字）。
3. 传完刷新，那个包会显示「正在用」。
4. 打开你的 `/download` 页面确认按钮指向你自己的域名。

不想用就什么都别传，行为和以前完全一样，下载仍然走上游。

三个桌面安装包加起来约 320MB，**每个用户下载都走你的出口带宽** —— 这一点自己权衡。

---

## 部署

把 `by-wave-calendar-v1.7.2.tar.gz` 和 `.sig` 一起传到后台「系统更新」。
数据库没有新迁移，`.env` 不用改。

### 传完验一下

```bash
curl -s https://你的域名/health
```

看到 `{"status":"ok","version":"1.7.2"}` 就成了。

后台左边 **运维** 组里的 **客户端更新** 打开，能看到「服务器上的安装包」那一块就说明升对了。

---

## OA

这版没改 OA，装过就不用再装。
